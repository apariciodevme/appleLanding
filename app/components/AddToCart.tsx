"use client";

import React from "react";

const AddToCart = () => {
  return (
    <div>
      <button className="btn btn-primary"  onClick={() => console.log("shit")}>Add to Cart</button>
    </div>
  );
};

export default AddToCart;
